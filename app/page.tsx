import { WorkspaceLayout } from '@/components/layout';

/**
 * @page Dashboard
 * @description AI-First Development Dashboard - v0.app style 3-panel layout
 * @ai-context Ana kontrol paneli - sol chat (prompt engine), ortada dashboard, sag chat (automation)
 */

export default function DashboardPage() {
  return <WorkspaceLayout />;
}
